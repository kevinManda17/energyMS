# Dépendances pour Python 3.14+ (Windows inclus).
#
# TensorFlow ne publie pas encore de wheels pour Python 3.14 : il est donc
# exclu de ce fichier. Conséquence : les modèles Keras (GRU/LSTM) ne sont pas
# chargés — le service de prévision bascule automatiquement sur les modèles
# scikit-learn (le chargement Keras est protégé par try/except dans
# apps/forecasting/services.py). Pour un support ML complet, utilisez
# Python 3.11 ou 3.12 avec requirements.txt.
Django>=4.2,<5.1
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
django-filter>=24.0
drf-spectacular>=0.27
psycopg2-binary>=2.9
dj-database-url>=2.2
python-dotenv>=1.0
paho-mqtt>=1.6
pandas>=2.2
scikit-learn>=1.5
numpy>=1.26
# Optionnel : le moteur flou est en Python pur (cf. cahier des charges).
# Décommentez pour une variante basée scikit-fuzzy.
# scikit-fuzzy>=0.4.2
joblib>=1.4
requests>=2.32
gunicorn>=22.0
# dev / tests
pytest>=8.0
pytest-django>=4.8
factory_boy>=3.3
